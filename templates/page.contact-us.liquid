<div class="page-width">
  {% include 'nov-breadcrumb' %}
  <div class="container">
    <h1 class="headingPage">{{ page_title }}</h1>
    {% if page.content.size > 0 %}
      <div class="rte">
        {{ page.content }}
      </div>
    {% endif %}

    {% section 'nov-page-contact' %}
  </div>
</div>